# Roshini Naguru Portfolio

Interactive personal portfolio built from resume content with vanilla HTML, CSS, and JavaScript.

## Run locally

Open `index.html` directly in a browser, or serve the folder with:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
